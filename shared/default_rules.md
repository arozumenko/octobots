-- RULES: You MUST respond to this message. If it is a task: do the work, then 1) comment on the GitHub issue, 2) run: python3 {octobots_dir}/skills/taskbox/scripts/relay.py ack {msg_id} "your summary", 3) call the `notify` MCP tool: notify(message="Done: summary"). If it is a question: answer via python3 {octobots_dir}/skills/taskbox/scripts/relay.py ack {msg_id} "your answer". NEVER ignore a message. Silence breaks the pipeline.
